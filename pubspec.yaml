name: flutter_permission_wizard
description: "Declarative permission wizard for Flutter: themeable rationale, denial, restricted, and settings flows built on permission_handler."
version: 0.1.4
homepage: https://github.com/iuzairaslam/flutter_permission_wizard
repository: https://github.com/iuzairaslam/flutter_permission_wizard
issue_tracker: https://github.com/iuzairaslam/flutter_permission_wizard/issues
topics:
  - permissions
  - permission-handler
  - ui
  - wizard

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.0
  app_settings: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.0

flutter:
